---
import hero from '../data/home.json';
const { className = '' } = Astro.props;
---

<a
  href={hero.hero.cta.link}
  class={`fade-up ${className} w-fit bg-primary action-hover text-dark mt-4 inline-block bg-black text-white px-6 py-2`}
  >{hero.hero.cta.text}</a
>
